# 🏗️ АРХИТЕКТУРА BZR (BEZBEDNOST I ZDRAVLJE NA RADU) СИСТЕМА

## 🎯 ПРЕГЛЕД СИСТЕМА

**AI Botislav BZR Expert** - Интелигентни систем за процену ризика и креирање Акта о процени ризика

---

## 📦 КОМПОНЕНТЕ СИСТЕМА

### 1️⃣ **База знања - Радна места**
**Фајл:** `knowledge-base/positions.json`

**Структура:**
```typescript
interface Position {
  positionNumber: string;        // "1", "2", "3"...
  title: string;                 // "ИЗВРШНИ ДИРЕКТОР ЗА ОПЕРАТИВНЕ ПОСЛОВЕ"
  description: string;           // Детаљан опис послова
  responsibilities: string[];    // Одговорности
  hazards: string[];            // Идентификоване опасности
  reportingTo: string;          // Коме је одговоран
  sector: string;               // РЈ/Сектор
  workEnvironment?: {           // Радна околина (ће се попунити кроз разговор)
    location: 'office' | 'field' | 'hybrid';
    indoor: boolean;
    outdoor: boolean;
    publicAreas: boolean;
    traffic: boolean;
  };
  equipment?: string[];         // Опрема и машине
  materials?: string[];         // Материјали са којима ради
}
```

**Извор:** 137 позиција екстраховано из "Опис послова"

---

### 2️⃣ **База знања - Запослени**
**Фајл:** `knowledge-base/employees.json`

**Структура:**
```typescript
interface Employee {
  id: string;                   // Јединствени ID
  firstName: string;
  lastName: string;
  positionNumber: string;       // Веза са positions.json
  sector: string;
  department: string;
  hireDate: string;
  healthStatus?: {
    lastCheckup: string;
    restrictions: string[];
  };
}
```

**Извор:** Биће екстраховано из "Списак радних места са именима"

---

### 3️⃣ **База знања - Каталог ризика**
**Фајл:** `knowledge-base/risk-catalog.json`

**Структура:**
```typescript
interface RiskCategory {
  id: string;
  name: string;                 // Механички ризици, Хемијски, Биолошки...
  risks: Risk[];
}

interface Risk {
  id: string;
  code: string;                 // M01, M02, H01, H02...
  name: string;
  description: string;
  category: string;
  possibleConsequences: string[];
  preventiveMeasures: string[];
  requiredPPE: string[];       // Лична заштитна опрема
  legalBasis: string[];        // Законска регулатива
}
```

**Категорије ризика:**
1. Механички ризици (M)
2. Хемијски ризици (H)
3. Биолошки ризици (B)
4. Физички ризици (F)
5. Ергономски ризици (E)
6. Психосоцијални ризици (P)
7. Ризици од пожара и експлозије (FE)
8. Електрични ризици (EL)
9. Ризици од рада на висини (H)
10. Саобраћајни ризици (T)

---

### 4️⃣ **AI Agent - Botislav BZR Expert**

**Persona:**
```
Име: Botislav
Улога: Стручњак за безбедност и здравље на раду
Искуство: 30 година у области БЗР
Специјализација: Процена ризика, Акти о процени ризика

Способности:
- Анализира опис послова
- Идентификује потенцијалне ризике
- Води разговор са координатором
- Поставља додатна питања
- Процењује вероватноћу и тежину ризика
- Предлаже превентивне мере
- Генерише комплетан Акт о процени ризика
```

---

## 🤖 МУЛТИ-АГЕНТСКИ СИСТЕМ

### **Agent Pipeline:**

```
┌──────────────────────────────────────────────────────────┐
│  1. DOCUMENT PROCESSOR AGENT                             │
│  - Паралелна обрада chunk-ова (Claude Sonnet 4.5)       │
│  - Екстракција радних места                             │
│  - Екстракција запослених                               │
└────────────────┬─────────────────────────────────────────┘
                 │
                 ▼
┌──────────────────────────────────────────────────────────┐
│  2. RISK ANALYSIS AGENT (Botislav)                      │
│  - Анализира опис позиције                              │
│  - Идентификује иницијалне ризике                       │
│  - Генерише додатна питања                             │
└────────────────┬─────────────────────────────────────────┘
                 │
                 ▼
┌──────────────────────────────────────────────────────────┐
│  3. CONVERSATION AGENT                                   │
│  - Води интерактивни разговор са корисником             │
│  - Поставља питања о радној околини                    │
│  - Прикупља додатне информације                        │
└────────────────┬─────────────────────────────────────────┘
                 │
                 ▼
┌──────────────────────────────────────────────────────────┐
│  4. RISK ASSESSMENT AGENT                                │
│  - Процењује вероватноћу (1-5)                         │
│  - Процењује тежину последица (1-5)                    │
│  - Израчунава ниво ризика (В × Т)                      │
│  - Категоризује ризике (низак/средњи/висок)           │
└────────────────┬─────────────────────────────────────────┘
                 │
                 ▼
┌──────────────────────────────────────────────────────────┐
│  5. PREVENTIVE MEASURES AGENT                            │
│  - Предлаже превентивне мере                            │
│  - Одређује приоритете                                  │
│  - Препоручује ЛЗО                                      │
│  - Дефинише обуке                                       │
└────────────────┬─────────────────────────────────────────┘
                 │
                 ▼
┌──────────────────────────────────────────────────────────┐
│  6. DOCUMENT GENERATION AGENT                            │
│  - Генерише Акт о процени ризика                        │
│  - Креира Word документ                                 │
│  - Генерише табеле и графиконе                          │
│  - Форматира по стандарду                               │
└──────────────────────────────────────────────────────────┘
```

---

## 💬 CONVERSATION FLOW - Botislav BZR Expert

### **Почетак разговора:**
```
Botislav: Здраво! Ја сам Botislav, стручњак за безбедност и здравље
          на раду са 30 година искуства. Помоћи ћу Вам да направимо
          детаљну процену ризика за радно место: [ПОЗИЦИЈА].

          Прочитао сам опис послова и идентификовао сам неке
          иницијалне ризике, али морам да Вам поставим неколико
          питања да бих био сигуран да ништа нисам пропустио.

          Да ли сте спремни да почнемо?
```

### **Питања по категоријама:**

#### **1. Радно окружење**
```typescript
const environmentQuestions = [
  {
    condition: (position) => !position.workEnvironment,
    questions: [
      "Да ли се рад обавља углавном у канцеларији, на терену, или комбиновано?",
      "Да ли запослени ради на отвореном простору?",
      "Да ли запослени ради у јавним просторима (паркови, пијаце, гробља)?",
      "Да ли је запослени изложен саобраћају?"
    ]
  }
];
```

#### **2. Физичко окружење**
```typescript
const physicalEnvironmentQuestions = [
  {
    condition: (position) => position.workEnvironment?.outdoor || hasKeyword(position, ['терен', 'одржавање']),
    questions: [
      "Да ли запослени ради на клизавим површинама (мокри подови, лед)?",
      "Да ли постоје степенице или мердевине на радном месту?",
      "Да ли запослени ради на висини? Ако да, на којој висини?",
      "Каква је осветљеност радног простора?"
    ]
  }
];
```

#### **3. Климатизација и вентилација**
```typescript
const hvacQuestions = [
  {
    condition: (position) => position.workEnvironment?.indoor,
    questions: [
      "Да ли постоји систем грејања? Каква је температура у зимском периоду?",
      "Да ли постоји систем климатизације? Каква је температура лети?",
      "Да ли је простор адекватно проветраван?",
      "Да ли постоје извори прашине или аерозола?"
    ]
  }
];
```

#### **4. Опрема и машине**
```typescript
const equipmentQuestions = [
  {
    condition: (position) => hasKeyword(position, ['машина', 'возило', 'алат']),
    questions: [
      "Које тачно машине користи запослени?",
      "Да ли су машине опремљене заштитним уређајима?",
      "Колико често се врши одржавање опреме?",
      "Да ли запослени користи ручни алат? Који?"
    ]
  }
];
```

#### **5. Материјали**
```typescript
const materialsQuestions = [
  {
    condition: (position) => hasKeyword(position, ['хемикалије', 'материјал', 'ђубриво', 'пестицид']),
    questions: [
      "Са којим хемикалијама запослени ради?",
      "Да ли постоје безбедносни листови за све хемикалије?",
      "Како се складиште хемикалије?",
      "Да ли запослени има обуку за руковање хемикалијама?"
    ]
  }
];
```

#### **6. Психосоцијални фактори**
```typescript
const psychosocialQuestions = [
  {
    condition: (position) => hasKeyword(position, ['директор', 'руководилац', 'шеф']),
    questions: [
      "Какав је ниво стреса на овој позицији (1-5)?",
      "Да ли запослени ради под временским притиском?",
      "Колико сати дневно у просеку ради запослени?",
      "Да ли ради у сменама? Да ли ради ноћу?"
    ]
  }
];
```

---

## 📈 ПРОЦЕНА РИЗИКА - Методологија

### **Матрица ризика:**
```
Вероватноћа (В):
1 = Веома мала (једном у 10+ година)
2 = Мала (једном у 5-10 година)
3 = Средња (једном у 1-5 година)
4 = Велика (неколико пута годишње)
5 = Веома велика (недељно или чешће)

Тежина (Т):
1 = Безнaчајна (прва помоћ)
2 = Мања (амбулантна)
3 = Умерена (повреда са боловањем)
4 = Озбиљна (тешка повреда, дуго боловање)
5 = Катастрофална (смртни исход)

Ниво ризика (НР) = В × Т

Категорије:
- НР 1-4:   НИЗАК (зелено)
- НР 5-12:  СРЕДЊИ (жуто)
- НР 13-25: ВИСОК (црвено)
```

---

## 📋 ГЕНЕРИСАЊЕ АКТА О ПРОЦЕНИ РИЗИКА

### **Структура документа:**

```
1. ОСНОВНИ ПОДАЦИ О ПОСЛОДАВЦУ
   - Назив: ЈКП ЗЕЛЕНИЛО ПАНЧЕВО
   - Адреса
   - Делатност
   - Број запослених

2. РАДНО МЕСТО: [ПОЗИЦИЈА]
   2.1. Опис послова
   2.2. Услови рада
   2.3. Опрема и средства за рад
   2.4. Материјали

3. ИДЕНТИФИКОВАНИ РИЗИЦИ
   Табела:
   | Р.бр | Ризик | Категорија | В | Т | НР | Ниво |
   |------|-------|------------|---|---|----|------|

4. ПРЕВЕНТИВНЕ МЕРЕ
   За сваки ризик:
   - Техничке мере
   - Организационе мере
   - Лична заштитна опрема
   - Обуке

5. ПРОЦЕНА ПРЕОСТАЛОГ РИЗИКА
   (након примене мера)

6. ПЛАН АКТИВНОСТИ
   - Одговорна лица
   - Рокови
   - Трошкови

7. ПРИЛОЗИ
   - Безбедносни листови
   - Упутства
   - Сертификати
```

---

## 🗄️ БАЗА ПОДАТАКА

### **Табеле:**

```sql
-- Радна места
CREATE TABLE positions (
  id SERIAL PRIMARY KEY,
  position_number VARCHAR(10) UNIQUE,
  title VARCHAR(255),
  description TEXT,
  responsibilities JSONB,
  hazards JSONB,
  reporting_to VARCHAR(255),
  sector VARCHAR(255),
  work_environment JSONB,
  created_at TIMESTAMP DEFAULT NOW()
);

-- Запослени
CREATE TABLE employees (
  id SERIAL PRIMARY KEY,
  first_name VARCHAR(100),
  last_name VARCHAR(100),
  position_number VARCHAR(10) REFERENCES positions(position_number),
  sector VARCHAR(255),
  hire_date DATE,
  created_at TIMESTAMP DEFAULT NOW()
);

-- Каталог ризика
CREATE TABLE risk_catalog (
  id SERIAL PRIMARY KEY,
  code VARCHAR(10) UNIQUE,
  name VARCHAR(255),
  category VARCHAR(50),
  description TEXT,
  consequences JSONB,
  preventive_measures JSONB,
  required_ppe JSONB
);

-- Процене ризика
CREATE TABLE risk_assessments (
  id SERIAL PRIMARY KEY,
  position_number VARCHAR(10),
  risk_code VARCHAR(10),
  probability INT CHECK (probability BETWEEN 1 AND 5),
  severity INT CHECK (severity BETWEEN 1 AND 5),
  risk_level INT,
  status VARCHAR(20),
  assessed_by VARCHAR(100),
  assessed_at TIMESTAMP,
  conversation_log JSONB
);

-- Акти о процени ризика
CREATE TABLE risk_assessment_documents (
  id SERIAL PRIMARY KEY,
  position_number VARCHAR(10),
  document_number VARCHAR(50),
  status VARCHAR(20),
  file_path VARCHAR(255),
  created_by VARCHAR(100),
  created_at TIMESTAMP DEFAULT NOW(),
  approved_by VARCHAR(100),
  approved_at TIMESTAMP
);
```

---

## 🚀 ИМПЛЕМЕНТАЦИЈА

### **Следећи кораци:**

1. ✅ **Направити јединствену базу позиција** (merge резултати)
2. 🔄 **Екстраховати списак запослених** (нови документ)
3. 🔄 **Креирати каталог ризика**
4. 🔄 **Имплементирати Botislav BZR Expert агента**
5. 🔄 **Имплементирати conversation flow**
6. 🔄 **Имплементирати процену ризика**
7. 🔄 **Имплементирати генерисање докумената**

---

**Датум:** 2025-11-19
**Верзија:** 1.0
**Статус:** 🏗️ У изградњи
